##  #Echo

*Echo the contents of file*
```bash
echo "$(<my_file.txt)"
```

*Convert text to base64*
```bash
echo <text> | base64 -e
```

*Convert text from base64*
```bash
echo <text> | base64 -d
```
